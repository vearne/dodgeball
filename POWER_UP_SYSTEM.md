# 道具系统说明文档

## 概述
游戏中添加了完整的道具系统，包括三种道具类型，可在地图编辑器中配置。

## 道具类型

### 1. 血瓶 (Health)
- **图片**: `hp_potion_50_50.png` - 红色药水瓶 🧪
- **效果**: 增加1条生命值（不超过最大生命值）
- **持续时间**: 永久（立即生效）

### 2. 速度靴子 (Speed Boost)
- **图片**: `boot_50_50.png` - 棕色靴子 👢
- **效果**: 移动速度增加20%
- **持续时间**: 10秒

### 3. 攻速球 (Attack Speed)
- **图片**: `speed_ball_50_50.png` - 橙色带翅膀的球 ⚡
- **效果**: 投掷冷却时间减半
- **持续时间**: 30秒

## 地图配置

### 在地图编辑器中配置道具

1. **选择道具类型**
   - 在地图编辑器的"道具配置"区域
   - 勾选希望在地图中出现的道具类型
   - 可以选择多个道具类型

2. **设置生成间隔**
   - 输入道具生成间隔时间（秒）
   - 范围：10-300秒
   - 默认值：30秒

3. **保存地图**
   - 点击保存按钮
   - 道具配置会与地图一起保存

### JSON 配置格式

```json
{
  "id": "custom_map_1",
  "name": "示例地图",
  "description": "带道具的地图",
  "enemyCount": 3,
  "obstacles": [...],
  "allowedPowerUps": ["health", "speedBoost", "attackSpeed"],
  "powerUpSpawnInterval": 30.0
}
```

## 道具生成机制

### 生成规则

1. **时间间隔**
   - 根据地图配置的 `powerUpSpawnInterval` 定期生成
   - 如果地图未配置道具（`allowedPowerUps` 为空），则不生成

2. **位置选择**
   - 只在可玩区域内生成
   - 不会在障碍物上生成
   - 不会在玩家位置附近生成（至少保持10像素距离）
   - 最多尝试50次寻找有效位置

3. **道具类型**
   - 从地图配置的 `allowedPowerUps` 中随机选择
   - 每次生成时随机决定道具类型

4. **拾取机制**
   - 玩家碰撞到道具时自动拾取
   - 道具效果立即生效
   - 道具消失

### 特殊情况

- **击败敌人掉落**: 击败敌人时有30%概率掉落道具
- **道具叠加**: 多个同类型道具效果会重置持续时间（而不是叠加）

## 内置地图配置

### 关卡 1：新手训练
- 允许道具：血瓶
- 生成间隔：45秒

### 关卡 2：障碍挑战
- 允许道具：血瓶、速度靴子
- 生成间隔：40秒

### 关卡 3：终极考验
- 允许道具：血瓶、速度靴子、攻速球
- 生成间隔：35秒

## 技术实现

### 核心文件

1. **power_up_component.dart**
   - 道具组件实现
   - 道具渲染和碰撞检测
   - 道具效果应用

2. **mission_map.dart**
   - 地图数据结构
   - 道具配置字段

3. **mission_dodgeball_game.dart**
   - 道具生成逻辑
   - 位置验证
   - 效果管理

4. **map_editor_screen.dart**
   - 道具配置UI
   - 道具类型选择器

### 关键方法

- `_findValidPowerUpPosition()`: 查找有效的道具生成位置
- `_dropPowerUp()`: 生成道具
- `_applyPowerUp()`: 应用道具效果
- `applySpeedBoost()`: 应用速度提升效果
- `applyAttackSpeedBoost()`: 应用攻速提升效果

## 使用建议

1. **新手关卡**: 只配置血瓶，较长生成间隔（45秒以上）
2. **中级关卡**: 配置血瓶和速度靴子，中等生成间隔（30-45秒）
3. **高级关卡**: 配置所有道具类型，较短生成间隔（20-30秒）
4. **挑战关卡**: 不配置道具，或只配置少量道具

## 注意事项

1. 道具不会在障碍物上生成
2. 道具不会在玩家附近生成，避免意外拾取
3. 如果地图过于拥挤（障碍物太多），可能无法生成道具
4. 建议生成间隔不要太短（< 15秒），避免道具过多影响游戏平衡

## 未来扩展

可以考虑添加以下道具类型：
- 护盾：短时间内免疫一次伤害
- 多重投掷：一次投出3个球
- 减速陷阱：减慢敌人移动速度
- 分身术：创建一个假分身吸引敌人

