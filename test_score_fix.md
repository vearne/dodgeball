# 多人联机模式得分显示修复测试

## 修复内容

### 1. 服务器端修复
- 在 `Player` 结构体中添加了 `Score` 字段
- 在 `RoomState` 中添加了 `RedScore` 和 `BlueScore` 字段
- 修复了队伍计数逻辑，`RedCount` 和 `BlueCount` 现在正确表示存活人数
- 在碰撞检测中添加了得分统计逻辑

### 2. 客户端修复
- 在 `RoomInfo` 类中添加了 `redScore` 和 `blueScore` 字段
- 修复了队伍统计显示逻辑：
  - 淘汰赛模式：显示存活人数（红队: X人，蓝队: Y人）
  - 限时赛模式：显示得分（红队: X分，蓝队: Y分）

## 测试步骤

### 1. 启动服务器
```bash
cd dodgeball-server
./start.sh
```

### 2. 测试淘汰赛模式
1. 启动客户端，选择多人模式 - 淘汰赛
2. 创建或加入房间
3. 观察队伍统计显示是否正确显示存活人数
4. 进行游戏，验证被击中的玩家是否被正确淘汰
5. 验证队伍存活人数是否正确更新

### 3. 测试限时赛模式
1. 启动客户端，选择多人模式 - 限时赛
2. 创建或加入房间
3. 观察队伍统计显示是否正确显示得分
4. 进行游戏，验证击中敌人是否正确增加得分
5. 验证队伍得分是否正确更新

## 预期结果

- 淘汰赛模式：队伍统计显示存活人数，格式为"红队: X人，蓝队: Y人"
- 限时赛模式：队伍统计显示得分，格式为"红队: X分，蓝队: Y分"
- 得分统计准确，击中敌人后立即更新
- 存活人数统计准确，玩家被击中后立即更新

## 注意事项

- 确保服务器和客户端都使用最新代码
- 如果仍有问题，检查网络连接和服务器日志
- 验证碰撞检测是否正常工作
