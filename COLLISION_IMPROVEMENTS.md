# 碰撞检测改进说明

## 问题描述
原来的碰撞检测系统存在以下问题：
1. 高速球可能在一帧内完全穿透玩家，导致碰撞检测失败
2. 只依赖箭头组件的碰撞检测，覆盖范围不够全面
3. 碰撞距离计算不够精确，容易出现误判

## 改进方案

### 1. 连续碰撞检测
- 在每一帧的`update`方法中添加连续碰撞检测
- 分析球在这一帧内的移动轨迹
- 计算轨迹与玩家的最近距离，防止穿透

### 2. 双重碰撞体
- 玩家既有圆形碰撞体（主要碰撞检测）
- 保留箭头组件的精确碰撞体（备用检测）
- 提供更可靠的碰撞检测覆盖

### 3. 优化的碰撞验证
- 使用更严格的碰撞距离（0.9倍玩家半径）
- 降低高速球检测阈值（120速度）
- 更严格的穿透检测（-0.15点积阈值）

### 4. 调试功能
- 可选的碰撞范围可视化
- 通过`DodgeballGame.showDebugInfo`控制
- 帮助开发者调试碰撞检测问题

## 使用方法

### 启用调试模式
```dart
// 在游戏初始化时设置
DodgeballGame.showDebugInfo = true;
```

### 自定义碰撞参数
```dart
// 在BallComponent中调整
final collisionDistance = ballRadius + player.radius * 0.9; // 碰撞距离
if (velocity.length > 120) // 高速球检测阈值
if (dotProduct < -0.15) // 穿透检测阈值
```

## 技术细节

### 连续碰撞检测算法
1. 计算球在上一帧和当前帧的位置
2. 使用线段到圆心的最近点算法
3. 如果最近距离小于球半径，判定为碰撞

### 碰撞优先级
1. 连续碰撞检测（最高优先级）
2. 玩家圆形碰撞体
3. 箭头组件精确碰撞体（备用）

## 性能考虑
- 连续碰撞检测会增加每帧的计算量
- 建议在需要高精度碰撞检测时启用
- 可以通过调整检测频率来平衡性能和精度

## 测试建议
1. 使用高速球测试穿透问题
2. 从不同角度投掷球验证碰撞检测
3. 启用调试模式观察碰撞范围
4. 测试边界情况（球擦边而过）
