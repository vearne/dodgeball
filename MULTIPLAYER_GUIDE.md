# 躲避球游戏多人模式使用指南

## 概述

躲避球游戏现在支持多人联机对战！你可以通过网络与其他玩家进行实时对战，体验更加刺激的游戏体验。

## 游戏特性

### 多人游戏模式
- **红队 vs 蓝队**: 每队最多6名玩家
- **实时对战**: 基于WebSocket的实时同步
- **AI填充**: 不足6人时自动添加AI玩家
- **跨平台**: 支持桌面和移动设备

### 游戏模式
1. **淘汰赛**: 
   - 可设置1-5条生命值
   - 被球击中扣除生命值
   - 生命值归零即被淘汰
   - 消灭对方全部玩家获胜

2. **限时赛**:
   - 可选择1分钟或3分钟
   - 击中对手得分
   - 时间结束时得分高的队伍获胜

### 游戏规则
- **投掷冷却**: 每次投掷后有10秒冷却时间
- **球的物理**: 球撞墙会反弹，撞墙次数减1，为0时消失
- **击中效果**: 球击中玩家立即消失，被击中的玩家受到伤害
- **移动限制**: 玩家只能在各自队伍区域内移动

## 安装和启动

### 1. 启动游戏服务器

首先需要启动游戏服务器：

```bash
# 进入服务器目录
cd dodgeball-server

# 使用启动脚本（推荐）
./start.sh

# 或者直接运行服务器
./server
```

服务器启动后会监听在 `http://localhost:8080`

### 2. 启动Flutter客户端

```bash
# 在项目根目录
flutter run
```

## 游戏操作指南

### 连接服务器

1. 打开游戏，选择"多人联机模式"
2. 在连接界面输入服务器地址（默认：`ws://localhost:8080/ws`）
3. 点击"连接服务器"
4. 输入玩家昵称
5. 点击"加入房间"

### 选择队伍

- 在房间大厅中选择加入"红队"或"蓝队"
- 每队最多6名玩家
- 可以看到其他玩家的状态（在线/离线、AI/真人）

### 开始游戏

- 任意玩家可以点击"开始游戏"按钮
- 游戏开始后所有玩家进入游戏界面

### 游戏控制

#### 桌面端控制
- **移动**: `WASD` 或方向键
- **投掷**: `空格键` 或鼠标点击瞄准投掷

#### 移动端控制
- **移动**: 虚拟摇杆
- **投掷**: 投掷按钮

### 游戏界面

- **顶部状态栏**: 显示队伍人数和游戏信息
- **网络状态指示器**: 右上角显示网络连接状态
- **队伍统计**: 左上角红队，右上角蓝队
  - 淘汰赛模式：显示存活人数
  - 限时赛模式：显示得分

## 技术架构

### 客户端-服务器架构
- **客户端**: Flutter游戏负责显示和输入
- **服务器**: Go语言服务器负责游戏逻辑和状态同步
- **通信**: WebSocket实时双向通信
- **权威服务器**: 所有游戏逻辑由服务器处理，确保公平性

### 网络同步
- **60FPS同步**: 游戏状态每秒同步60次
- **输入预测**: 本地输入立即响应，服务器验证
- **平滑插值**: 其他玩家位置使用插值平滑显示

## 故障排除

### 连接问题

**无法连接服务器**
- 检查服务器是否已启动
- 确认服务器地址正确（默认：`ws://localhost:8080/ws`）
- 检查防火墙设置
- 确保8080端口未被占用

**连接中断**
- 检查网络连接
- 服务器可能重启，尝试重新连接

### 游戏问题

**游戏卡顿**
- 检查网络延迟
- 关闭其他占用网络的应用

**玩家不同步**
- 这是正常现象，服务器会处理同步
- 如果严重不同步，尝试重新连接

### 性能优化

**提高游戏性能**
- 关闭其他应用程序
- 使用有线网络连接
- 确保设备性能充足

## 开发说明

### 服务器配置

可以通过环境变量配置服务器：

```bash
# Redis配置（可选，用于多房间支持）
export REDIS_ADDR=127.0.0.1:6379
export REDIS_DB=0
export REDIS_PASSWORD=""

# HTTP服务器配置
export HTTP_ADDR=:8080
```

### 游戏参数

服务器端的游戏参数在 `internal/room/loop.go` 中定义：

- `tickRateHz = 60`: 游戏循环帧率
- `playerSpeed = 120.0`: 玩家移动速度
- `ballSpeed = 400.0`: 球的速度
- `playerRadius = 15.0`: 玩家半径
- `ballRadius = 8.0`: 球半径

### 扩展功能

**添加新的游戏模式**
1. 在客户端 `GameplayMode` 枚举中添加新模式
2. 在服务器端实现对应的游戏逻辑
3. 更新UI界面支持新模式

**自定义房间**
- 服务器支持多房间，可以扩展房间配置功能
- 可以添加私人房间、观战模式等

## 许可证

MIT License

## 支持

如果遇到问题或有改进建议，请创建Issue或联系开发者。

---

享受你的多人躲避球游戏体验！🎮
